# Traefik values for Pi homelab.
service:
  type: NodePort
  ports:
    web:
      nodePort: 30080
    websecure:
      nodePort: 30443

ports:
  web:
    port: 8000
    expose:
      default: true  # Changed from 'true' to an object
    exposedPort: 80
  websecure:
    port: 8443
    expose:
      default: true  # Changed from 'true' to an object
    exposedPort: 443
  metrics:
    port: 9100
    expose:
      default: true  # Changed from 'true' to an object
    exposedPort: 9100

resources:
  requests:
    memory: 128Mi
    cpu: 100m
  limits:
    memory: 256Mi
    cpu: 300m

logs:
  access:
    enabled: true

api:
  dashboard: true
  insecure: true

metrics:
  prometheus:
    enabled: true
    entryPoint: metrics